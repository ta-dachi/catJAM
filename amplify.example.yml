version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
        - echo "REACT_APP_CLIENT_ID=39nae...." >> .env
        - echo "REACT_APP_SECRET=fkgitoptm....k" >> .env
        - echo "REACT_APP_REDIRECT_URI=https://master.d2kgyujnorlgrv.amplifyapp.com/chat >> .env
        - echo "REACT_APP_SCOPE_URI=chat%3Aread+user_read+user:read:follows+chat:edit" >> .env
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
